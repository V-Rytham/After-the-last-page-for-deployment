diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index 7f181a5..377324b 100644
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -16,10 +16,10 @@ import SettingsPage from './pages/SettingsPage';
 import BookQuiz from './pages/BookQuiz';
 import api from './utils/api';
 import { clearAuthSession, getStoredToken, getStoredUser, saveAuthSession, updateStoredUser } from './utils/auth';
+import { DEFAULT_UI_THEME, THEME_STORAGE_KEY, UI_THEMES } from './utils/uiThemes';
 import './index.css';
 
-const THEME_STORAGE_KEY = 'atlp-ui-theme';
-const VALID_THEMES = ['light', 'sepia', 'dark'];
+const VALID_THEMES = UI_THEMES.map((theme) => theme.id);
 
 const RequireMember = ({ currentUser, children }) => {
   const location = useLocation();
@@ -69,7 +69,7 @@ const App = () => {
   const bootstrapStartedRef = useRef(false);
   const [uiTheme, setUiTheme] = useState(() => {
     const storedTheme = window.localStorage.getItem(THEME_STORAGE_KEY);
-    return VALID_THEMES.includes(storedTheme) ? storedTheme : 'sepia';
+    return VALID_THEMES.includes(storedTheme) ? storedTheme : DEFAULT_UI_THEME;
   });
 
   useEffect(() => {
diff --git a/frontend/src/components/layout/Navbar.css b/frontend/src/components/layout/Navbar.css
index 7974bda..7d51103 100644
--- a/frontend/src/components/layout/Navbar.css
+++ b/frontend/src/components/layout/Navbar.css
@@ -9,6 +9,17 @@
   box-shadow: none;
 }
 
+.navbar.navbar-capsule {
+  width: min(100%, var(--page-max-width));
+  margin: 0.75rem auto 0;
+  padding: 0.56rem 1.05rem;
+  border-radius: 999px;
+  background: var(--nav-capsule-bg, var(--nav-bg));
+  border: 1px solid var(--nav-capsule-border, var(--border-soft));
+  box-shadow: var(--nav-capsule-shadow, var(--shadow-panel));
+  border-bottom: 0;
+}
+
 .navbar-container {
   width: min(100%, var(--page-max-width));
   margin: 0 auto;
@@ -20,12 +31,20 @@
   gap: 0.9rem;
 }
 
+.navbar.navbar-capsule .navbar-container {
+  width: 100%;
+  margin: 0;
+  min-height: 2.05rem;
+  padding: 0;
+  gap: 0.8rem;
+}
+
 .navbar-logo {
   display: inline-flex;
   align-items: center;
-  gap: 0.7rem;
+  gap: 0.55rem;
   color: var(--text-primary);
-  min-width: 12.5rem;
+  min-width: 11.5rem;
 }
 
 .logo-icon {
@@ -39,58 +58,82 @@
 }
 
 .logo-text {
-  font-size: 0.95rem;
+  font-size: 0.9rem;
   line-height: 1.15;
   font-weight: 500;
   letter-spacing: 0.02em;
 }
 
 .logo-tagline {
-  font-size: 0.68rem;
+  font-size: 0.62rem;
   letter-spacing: 0.12em;
   text-transform: uppercase;
   color: var(--text-muted);
   opacity: 0.9;
 }
 
-.navbar-links {
-  display: inline-flex;
+.nav-links-group {
+  display: flex;
   align-items: center;
+  justify-content: center;
   justify-self: center;
-  gap: 0.2rem;
+  border-radius: 999px;
+  background: var(--nav-group-bg, rgba(25, 35, 60, 0.55));
+  border: 1px solid var(--nav-group-border, rgba(120, 170, 255, 0.18));
+  padding: 0.28rem 0.65rem;
+  box-shadow: 0 1px 0 rgba(255, 255, 255, 0.03) inset;
+}
+
+.navbar-links {
+  display: flex;
+  align-items: center;
+  flex-wrap: wrap;
+  justify-content: center;
+  gap: 0.35rem 0.8rem;
 }
 
 .nav-link {
   display: inline-flex;
   align-items: center;
-  gap: 0.45rem;
-  min-height: 2.15rem;
-  padding: 0 0.75rem;
+  gap: 0.4rem;
+  min-height: 2.28rem;
+  padding: 0.32rem 0.78rem;
   border-radius: 999px;
   color: var(--text-secondary);
-  font-size: 0.9rem;
-  transition: background-color var(--transition-fast), color var(--transition-fast);
+  font-size: 0.84rem;
+  font-weight: 500;
+  transition:
+    background-color var(--transition-fast),
+    border-color var(--transition-fast),
+    color var(--transition-fast),
+    box-shadow var(--transition-fast),
+    transform var(--transition-fast);
+  border: 1px solid transparent;
 }
 
-.nav-link:hover,
-.nav-link.active {
+.nav-link:hover {
   color: var(--text-primary);
-  background: var(--surface-muted);
+  background: var(--nav-link-hover-bg, var(--surface-muted));
+  box-shadow: 0 8px 18px rgba(28, 20, 12, 0.06);
+  transform: translateY(-1px);
 }
 
 .nav-link.active {
-  box-shadow: 0 1px 0 rgba(28, 22, 15, 0.08);
+  color: var(--text-primary);
+  background: var(--nav-link-active-bg, var(--surface-muted));
+  border-color: var(--nav-link-active-border, var(--border-soft));
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.18);
 }
 
 .nav-link-label {
   white-space: nowrap;
 }
 
-.navbar-auth {
+.navbar-user-capsule {
   display: inline-flex;
   align-items: center;
   justify-self: end;
-  gap: 0.65rem;
+  gap: 0.5rem;
 }
 
 .sr-only {
@@ -111,7 +154,7 @@
 }
 
 .theme-trigger-icon {
-  padding: 0 0.65rem;
+  padding: 0 0.55rem;
 }
 
 .theme-option-row {
@@ -129,8 +172,8 @@
   display: inline-flex;
   align-items: center;
   justify-content: center;
-  min-height: 2.15rem;
-  padding: 0 0.65rem;
+  min-height: 2.28rem;
+  padding: 0 0.55rem;
   border-radius: 999px;
   border: 1px solid transparent;
   background: transparent;
@@ -198,8 +241,8 @@
   display: inline-flex;
   align-items: center;
   justify-content: center;
-  width: 2.35rem;
-  height: 2.35rem;
+  width: 2.15rem;
+  height: 2.15rem;
   border-radius: 999px;
   border: 1px solid var(--border-soft);
   background: var(--surface-1);
@@ -219,7 +262,7 @@
 }
 
 .profile-avatar {
-  font-size: 0.78rem;
+  font-size: 0.74rem;
   font-weight: 600;
   letter-spacing: 0.02em;
   color: var(--text-secondary);
@@ -266,16 +309,21 @@
   display: inline-flex;
   align-items: center;
   gap: 0.4rem;
-  min-height: 2.15rem;
-  padding: 0 0.75rem;
+  min-height: 2.28rem;
+  padding: 0 0.6rem;
   border-radius: 999px;
   border: 1px solid var(--border-soft);
   background: var(--surface-1);
   color: var(--text-secondary);
-  font-size: 0.85rem;
+  font-size: 0.82rem;
   transition: background-color var(--transition-fast), border-color var(--transition-fast), color var(--transition-fast);
 }
 
+.theme-trigger-label {
+  color: var(--text-secondary);
+  font-weight: 500;
+}
+
 .theme-trigger:hover {
   background: var(--surface-muted);
   color: var(--text-primary);
@@ -416,7 +464,7 @@
   }
 
   .nav-link {
-    padding: 0 0.65rem;
+    padding: 0.28rem 0.65rem;
   }
 }
 
@@ -434,7 +482,7 @@
   }
 
   .nav-link {
-    padding: 0 0.7rem;
+    padding: 0.28rem 0.7rem;
   }
 
   .theme-trigger-label {
@@ -459,8 +507,8 @@
     grid-template-columns: auto auto;
   }
 
-  .navbar-links,
-  .navbar-auth {
+  .nav-links-group,
+  .navbar-user-capsule {
     display: none;
   }
 
diff --git a/frontend/src/components/layout/Navbar.jsx b/frontend/src/components/layout/Navbar.jsx
index d0e4a81..ad70cd3 100644
--- a/frontend/src/components/layout/Navbar.jsx
+++ b/frontend/src/components/layout/Navbar.jsx
@@ -13,6 +13,7 @@ import {
   MoreHorizontal,
   Moon,
   PenLine,
+  Sparkles,
   Settings,
   Sun,
   User,
@@ -20,13 +21,20 @@ import {
   UserPlus,
   UsersRound,
 } from 'lucide-react';
+import { UI_THEMES } from '../../utils/uiThemes';
 import './Navbar.css';
 
-const themeOptions = [
-  { id: 'light', label: 'Light', icon: Sun },
-  { id: 'sepia', label: 'Sepia', icon: BookOpenText },
-  { id: 'dark', label: 'Dark', icon: Moon },
-];
+const THEME_ICONS = {
+  light: Sun,
+  sepia: BookOpenText,
+  dark: Moon,
+  midnight: Sparkles,
+};
+
+const themeOptions = UI_THEMES.map((theme) => ({
+  ...theme,
+  icon: THEME_ICONS[theme.id] || Sun,
+}));
 
 const useCloseOnPointerDownOutside = (isOpen, wrapperRef, onClose) => {
   useEffect(() => {
@@ -49,6 +57,23 @@ const useCloseOnPointerDownOutside = (isOpen, wrapperRef, onClose) => {
   }, [isOpen, onClose, wrapperRef]);
 };
 
+const useCloseOnEscape = (isOpen, onClose) => {
+  useEffect(() => {
+    if (!isOpen) {
+      return undefined;
+    }
+
+    const handleKeyDown = (event) => {
+      if (event.key === 'Escape') {
+        onClose();
+      }
+    };
+
+    document.addEventListener('keydown', handleKeyDown);
+    return () => document.removeEventListener('keydown', handleKeyDown);
+  }, [isOpen, onClose]);
+};
+
 const ThemeMenu = ({ uiTheme, onThemeChange, isOpen, onToggle, onClose, className = '' }) => {
   const wrapperRef = useRef(null);
   useCloseOnPointerDownOutside(isOpen, wrapperRef, onClose);
@@ -65,9 +90,10 @@ const ThemeMenu = ({ uiTheme, onThemeChange, isOpen, onToggle, onClose, classNam
         aria-haspopup="listbox"
         aria-expanded={isOpen}
       >
-        <ActiveIcon size={18} strokeWidth={2.1} aria-hidden="true" />
+        <ActiveIcon size={16} strokeWidth={2.1} aria-hidden="true" />
+        <span className="theme-trigger-label">{activeOption.label}</span>
         <span className="sr-only">Theme</span>
-        <ChevronDown size={16} aria-hidden="true" />
+        <ChevronDown size={14} aria-hidden="true" />
       </button>
 
       {isOpen && (
@@ -131,6 +157,7 @@ const MoreMenu = ({ isOpen, isActive, onToggle, onClose }) => {
 const ProfileMenu = ({ displayName, username, onLogout, isOpen, onToggle, onClose }) => {
   const wrapperRef = useRef(null);
   useCloseOnPointerDownOutside(isOpen, wrapperRef, onClose);
+  useCloseOnEscape(isOpen, onClose);
   const initials = useMemo(() => {
     const base = (displayName || username || 'Reader').trim();
     return base ? base[0].toUpperCase() : 'R';
@@ -188,12 +215,12 @@ const Navbar = ({ currentUser, onLogout, uiTheme, onThemeChange }) => {
   const [isProfileOpen, setIsProfileOpen] = useState(false);
 
   const isMember = Boolean(currentUser && !currentUser.isAnonymous);
-  const navIconProps = useMemo(() => ({ size: 18, strokeWidth: 2.1 }), []);
+  const navIconProps = useMemo(() => ({ size: 16, strokeWidth: 2.1 }), []);
 
   const primaryNavLinks = useMemo(
     () => [
       ...(isMember ? [
-        { path: '/desk', icon: <LibraryBig {...navIconProps} />, label: 'The Desk' },
+        { path: '/desk', icon: <LibraryBig {...navIconProps} />, label: 'Your Desk' },
         { path: '/library', icon: <BookOpen {...navIconProps} />, label: 'Library' }
       ] : []),
       { path: '/meet', icon: <UsersRound {...navIconProps} />, label: 'Meet' },
@@ -222,52 +249,54 @@ const Navbar = ({ currentUser, onLogout, uiTheme, onThemeChange }) => {
   };
 
   return (
-    <nav className="navbar glass-panel">
+    <nav className="navbar navbar-capsule">
       <div className="navbar-container">
         <Link to="/" className="navbar-logo" aria-label="After The Last Page">
-          <BookOpen className="logo-icon" size={18} strokeWidth={2.1} />
+          <BookOpen className="logo-icon" size={16} strokeWidth={2.1} />
           <div className="logo-copy">
             <span className="logo-text font-serif">After The Last Page</span>
             <span className="logo-tagline">Where books become conversations</span>
           </div>
         </Link>
 
-        <div className="navbar-links" aria-label="Primary">
-          {primaryNavLinks.map((link) => (
+        <div className="nav-links-group" aria-label="Primary">
+          <div className="navbar-links">
+            {primaryNavLinks.map((link) => (
+              <Link
+                key={link.path}
+                to={link.path}
+                className={`nav-link ${location.pathname.startsWith(link.path) ? 'active' : ''}`}
+                aria-label={link.label}
+              >
+                {link.icon}
+                <span className="nav-link-label">{link.label}</span>
+              </Link>
+            ))}
+
             <Link
-              key={link.path}
-              to={link.path}
-              className={`nav-link ${location.pathname.startsWith(link.path) ? 'active' : ''}`}
-              aria-label={link.label}
+              key={studioNavLink.path}
+              to={studioNavLink.path}
+              className={`nav-link nav-link-secondary ${location.pathname.startsWith(studioNavLink.path) ? 'active' : ''}`}
+              aria-label={studioNavLink.label}
             >
-              {link.icon}
-              <span className="nav-link-label">{link.label}</span>
+              {studioNavLink.icon}
+              <span className="nav-link-label">{studioNavLink.label}</span>
             </Link>
-          ))}
-
-          <Link
-            key={studioNavLink.path}
-            to={studioNavLink.path}
-            className={`nav-link nav-link-secondary ${location.pathname.startsWith(studioNavLink.path) ? 'active' : ''}`}
-            aria-label={studioNavLink.label}
-          >
-            {studioNavLink.icon}
-            <span className="nav-link-label">{studioNavLink.label}</span>
-          </Link>
 
-          <MoreMenu
-            isOpen={isMoreOpen}
-            isActive={location.pathname.startsWith(studioNavLink.path)}
-            onToggle={() => {
-              setIsMoreOpen((open) => !open);
-              setIsThemeOpen(false);
-              setIsProfileOpen(false);
-            }}
-            onClose={() => setIsMoreOpen(false)}
-          />
+            <MoreMenu
+              isOpen={isMoreOpen}
+              isActive={location.pathname.startsWith(studioNavLink.path)}
+              onToggle={() => {
+                setIsMoreOpen((open) => !open);
+                setIsThemeOpen(false);
+                setIsProfileOpen(false);
+              }}
+              onClose={() => setIsMoreOpen(false)}
+            />
+          </div>
         </div>
 
-        <div className="navbar-auth">
+        <div className="navbar-user-capsule">
           <ThemeMenu
             uiTheme={uiTheme}
             onThemeChange={onThemeChange}
@@ -278,6 +307,7 @@ const Navbar = ({ currentUser, onLogout, uiTheme, onThemeChange }) => {
               setIsMoreOpen(false);
             }}
             onClose={() => setIsThemeOpen(false)}
+            className="is-primary"
           />
 
           {currentUser ? (
@@ -318,22 +348,22 @@ const Navbar = ({ currentUser, onLogout, uiTheme, onThemeChange }) => {
               <span>Sign in</span>
             </Link>
           )}
-
-          <button
-            type="button"
-            className="mobile-menu-btn"
-            onClick={() => {
-              setIsMenuOpen((open) => !open);
-              setIsThemeOpen(false);
-              setIsMoreOpen(false);
-              setIsProfileOpen(false);
-            }}
-            aria-expanded={isMenuOpen}
-            aria-label="Open menu"
-          >
-            <Menu size={18} strokeWidth={2.1} aria-hidden="true" />
-          </button>
         </div>
+
+        <button
+          type="button"
+          className="mobile-menu-btn"
+          onClick={() => {
+            setIsMenuOpen((open) => !open);
+            setIsThemeOpen(false);
+            setIsMoreOpen(false);
+            setIsProfileOpen(false);
+          }}
+          aria-expanded={isMenuOpen}
+          aria-label="Open menu"
+        >
+          <Menu size={18} strokeWidth={2.1} aria-hidden="true" />
+        </button>
       </div>
 
 
diff --git a/frontend/src/index.css b/frontend/src/index.css
index 8cb9de3..44046af 100644
--- a/frontend/src/index.css
+++ b/frontend/src/index.css
@@ -57,6 +57,7 @@ html[data-theme='light'] {
   --nav-bg: rgba(246, 243, 237, 0.9);
   --surface-1: #fbf8f2;
   --surface-2: #f1ebdf;
+  --surface-2-elevated: var(--surface-2);
   --surface-muted: rgba(60, 47, 28, 0.05);
   --text-primary: #1c1c1c;
   --text-secondary: #5b544c;
@@ -68,10 +69,20 @@ html[data-theme='light'] {
   --accent-soft-strong: #dde3f0;
   --accent-border: rgba(63, 73, 100, 0.24);
   --accent-warm: #8b6e38;
+  --accent: var(--accent-warm);
   --success-soft: #e6efe5;
   --warning-soft: #f2ead6;
   --danger-soft: #f5e2e0;
   --selection: rgba(63, 73, 100, 0.18);
+
+  --nav-capsule-bg: linear-gradient(to right, var(--surface-1), var(--surface-2));
+  --nav-capsule-border: var(--border-soft);
+  --nav-capsule-shadow: 0 8px 22px rgba(47, 35, 20, 0.06), 0 1px 0 rgba(255, 255, 255, 0.34) inset;
+  --nav-group-bg: var(--surface-muted);
+  --nav-group-border: var(--border-soft);
+  --nav-link-hover-bg: var(--surface-muted);
+  --nav-link-active-bg: var(--accent-soft);
+  --nav-link-active-border: var(--accent-border);
 }
 
 html[data-theme='sepia'] {
@@ -80,6 +91,7 @@ html[data-theme='sepia'] {
   --nav-bg: rgba(239, 227, 194, 0.9);
   --surface-1: #f4ebd1;
   --surface-2: #eadbb8;
+  --surface-2-elevated: var(--surface-2);
   --surface-muted: rgba(73, 49, 24, 0.06);
   --text-primary: #2a1e12;
   --text-secondary: #5c4731;
@@ -91,10 +103,20 @@ html[data-theme='sepia'] {
   --accent-soft-strong: #d2d8e5;
   --accent-border: rgba(70, 80, 107, 0.24);
   --accent-warm: #8a6b34;
+  --accent: var(--accent-warm);
   --success-soft: #dfe8dd;
   --warning-soft: #ecdfbe;
   --danger-soft: #efd8d2;
   --selection: rgba(70, 80, 107, 0.2);
+
+  --nav-capsule-bg: linear-gradient(to right, var(--surface-1), var(--surface-2));
+  --nav-capsule-border: var(--border-soft);
+  --nav-capsule-shadow: 0 8px 22px rgba(47, 35, 20, 0.08), 0 1px 0 rgba(255, 255, 255, 0.28) inset;
+  --nav-group-bg: var(--surface-muted);
+  --nav-group-border: var(--border-soft);
+  --nav-link-hover-bg: var(--surface-muted);
+  --nav-link-active-bg: var(--accent-soft);
+  --nav-link-active-border: var(--accent-border);
 }
 
 html[data-theme='dark'] {
@@ -103,6 +125,7 @@ html[data-theme='dark'] {
   --nav-bg: rgba(17, 17, 17, 0.88);
   --surface-1: #191816;
   --surface-2: #22201d;
+  --surface-2-elevated: var(--surface-2);
   --surface-muted: rgba(229, 229, 229, 0.06);
   --text-primary: #e5e5e5;
   --text-secondary: #bab2a7;
@@ -114,12 +137,58 @@ html[data-theme='dark'] {
   --accent-soft-strong: #303952;
   --accent-border: rgba(215, 222, 244, 0.18);
   --accent-warm: #af9462;
+  --accent: var(--accent-warm);
   --success-soft: #203025;
   --warning-soft: #3a3220;
   --danger-soft: #3a2525;
   --selection: rgba(215, 222, 244, 0.18);
   --shadow-line: 0 1px 0 rgba(0, 0, 0, 0.35);
   --shadow-panel: 0 18px 44px rgba(0, 0, 0, 0.22);
+
+  --nav-capsule-bg: linear-gradient(180deg, var(--surface-2), var(--surface-1));
+  --nav-capsule-border: var(--border-soft);
+  --nav-capsule-shadow: 0 14px 30px rgba(0, 0, 0, 0.28), 0 1px 0 rgba(255, 255, 255, 0.03) inset;
+  --nav-group-bg: var(--surface-muted);
+  --nav-group-border: var(--border-soft);
+  --nav-link-hover-bg: var(--surface-muted);
+  --nav-link-active-bg: var(--accent-soft);
+  --nav-link-active-border: var(--accent-border);
+}
+
+html[data-theme='midnight'] {
+  --page-bg: #0b1020;
+  --page-bg-alt: #101a33;
+  --nav-bg: rgba(11, 16, 32, 0.86);
+  --surface-1: #101a33;
+  --surface-2: #152244;
+  --surface-2-elevated: var(--surface-2);
+  --surface-muted: rgba(231, 236, 255, 0.06);
+  --text-primary: #e7ecff;
+  --text-secondary: #bcc6ea;
+  --text-muted: #8c99c4;
+  --border-soft: rgba(231, 236, 255, 0.12);
+  --border-strong: rgba(231, 236, 255, 0.22);
+  --accent-warm: #79a6ff;
+  --accent: var(--accent-warm);
+  --accent-ink: #cfe0ff;
+  --accent-soft: rgba(121, 166, 255, 0.16);
+  --accent-soft-strong: rgba(121, 166, 255, 0.22);
+  --accent-border: rgba(121, 166, 255, 0.24);
+  --success-soft: rgba(118, 214, 160, 0.14);
+  --warning-soft: rgba(224, 184, 101, 0.16);
+  --danger-soft: rgba(229, 142, 122, 0.16);
+  --selection: rgba(121, 166, 255, 0.18);
+  --shadow-line: 0 1px 0 rgba(0, 0, 0, 0.42);
+  --shadow-panel: 0 18px 44px rgba(0, 0, 0, 0.28);
+
+  --nav-capsule-bg: linear-gradient(180deg, rgba(21, 34, 68, 0.92), rgba(16, 26, 51, 0.92));
+  --nav-capsule-border: var(--border-soft);
+  --nav-capsule-shadow: 0 16px 34px rgba(0, 0, 0, 0.32), 0 1px 0 rgba(255, 255, 255, 0.03) inset;
+  --nav-group-bg: var(--surface-muted);
+  --nav-group-border: rgba(121, 166, 255, 0.16);
+  --nav-link-hover-bg: rgba(121, 166, 255, 0.1);
+  --nav-link-active-bg: rgba(121, 166, 255, 0.16);
+  --nav-link-active-border: rgba(121, 166, 255, 0.24);
 }
 
 * {
diff --git a/frontend/src/pages/BookQuiz.css b/frontend/src/pages/BookQuiz.css
index 4aa95b5..79d4e3d 100644
--- a/frontend/src/pages/BookQuiz.css
+++ b/frontend/src/pages/BookQuiz.css
@@ -1,19 +1,19 @@
 .quiz-page {
   max-width: 980px;
   margin: 0 auto;
-  padding: 2rem 1.25rem 3rem;
+  padding: clamp(1.35rem, 3vw, 2rem) var(--page-gutter) var(--section-space);
   display: grid;
   gap: 1.25rem;
 }
 
 .quiz-hero {
-  padding: 1.25rem 1.25rem 1rem;
+  padding: 1.1rem 1.15rem 1rem;
   display: grid;
   gap: 0.25rem;
 }
 
 .quiz-hero p {
-  color: var(--muted, rgba(255, 255, 255, 0.7));
+  color: var(--text-secondary);
   margin: 0;
 }
 
@@ -39,35 +39,38 @@
 .quiz-option {
   text-align: left;
   width: 100%;
-  border: 1px solid rgba(255, 255, 255, 0.12);
+  border: 1px solid var(--border-soft);
   border-radius: 0.8rem;
   padding: 0.75rem 0.85rem;
-  background: rgba(0, 0, 0, 0.15);
+  background: var(--surface-muted);
   color: inherit;
   display: grid;
   grid-template-columns: 1.75rem 1fr;
   gap: 0.65rem;
   align-items: start;
   cursor: pointer;
+  transition: background-color var(--transition-fast), border-color var(--transition-fast), transform var(--transition-fast);
 }
 
 .quiz-option:hover {
-  border-color: rgba(255, 255, 255, 0.22);
+  border-color: var(--border-strong);
+  transform: translateY(-1px);
 }
 
 .quiz-option.selected {
-  border-color: rgba(255, 255, 255, 0.38);
-  background: rgba(255, 255, 255, 0.08);
+  border-color: var(--accent-border);
+  background: var(--accent-soft);
 }
 
 .quiz-option-letter {
   width: 1.65rem;
   height: 1.65rem;
   border-radius: 0.55rem;
-  background: rgba(255, 255, 255, 0.1);
+  background: color-mix(in srgb, var(--accent-soft) 70%, transparent);
   display: grid;
   place-items: center;
   font-weight: 600;
+  color: var(--accent-ink);
 }
 
 .quiz-option-text {
@@ -80,7 +83,7 @@
 
 .quiz-state.is-error p {
   margin: 0.5rem 0 0;
-  color: rgba(255, 255, 255, 0.8);
+  color: var(--text-secondary);
 }
 
 .quiz-error-title {
@@ -97,6 +100,57 @@
   flex-wrap: wrap;
 }
 
+.quiz-skeleton {
+  display: grid;
+  gap: 1rem;
+}
+
+.quiz-skeleton-card {
+  border-radius: var(--radius-lg);
+  border: 1px solid var(--border-soft);
+  background: var(--surface-1);
+  padding: 1.1rem;
+  overflow: hidden;
+  position: relative;
+}
+
+.quiz-skeleton-card::after {
+  content: '';
+  position: absolute;
+  inset: 0;
+  background: linear-gradient(
+    90deg,
+    transparent,
+    color-mix(in srgb, var(--surface-muted) 65%, transparent),
+    transparent
+  );
+  transform: translateX(-100%);
+  animation: quiz-shimmer 1.25s ease-in-out infinite;
+}
+
+.quiz-skeleton-line {
+  height: 0.9rem;
+  border-radius: 999px;
+  background: var(--surface-muted);
+}
+
+.quiz-skeleton-line.sm {
+  height: 0.7rem;
+  opacity: 0.85;
+}
+
+.quiz-skeleton-stack {
+  display: grid;
+  gap: 0.6rem;
+  margin-top: 0.9rem;
+}
+
+@keyframes quiz-shimmer {
+  to {
+    transform: translateX(100%);
+  }
+}
+
 .quiz-submit-row {
   display: flex;
   gap: 0.75rem;
@@ -105,4 +159,3 @@
   justify-content: flex-end;
   padding-top: 0.25rem;
 }
-
diff --git a/frontend/src/pages/BookQuiz.jsx b/frontend/src/pages/BookQuiz.jsx
index 8786d13..536815d 100644
--- a/frontend/src/pages/BookQuiz.jsx
+++ b/frontend/src/pages/BookQuiz.jsx
@@ -284,6 +284,21 @@ export default function BookQuiz() {
         </div>
       )}
 
+      {loading && (
+        <div className="quiz-skeleton" aria-hidden="true">
+          {[0, 1, 2].map((index) => (
+            <div key={index} className="quiz-skeleton-card">
+              <div className="quiz-skeleton-line" style={{ width: index === 0 ? '62%' : (index === 1 ? '54%' : '58%') }} />
+              <div className="quiz-skeleton-stack">
+                <div className="quiz-skeleton-line sm" style={{ width: '92%' }} />
+                <div className="quiz-skeleton-line sm" style={{ width: '86%' }} />
+                <div className="quiz-skeleton-line sm" style={{ width: '78%' }} />
+              </div>
+            </div>
+          ))}
+        </div>
+      )}
+
       {loading && processingMessage && (
         <div className="quiz-actions">
           <button type="button" className="btn-secondary" onClick={handleContinueToMeet} disabled={fallbackGranting}>
diff --git a/frontend/src/pages/BookThread.css b/frontend/src/pages/BookThread.css
index 0b009bf..1e5c8ac 100644
--- a/frontend/src/pages/BookThread.css
+++ b/frontend/src/pages/BookThread.css
@@ -2,8 +2,9 @@
   min-height: 100vh;
   padding: clamp(1.25rem, 3.5vw, 2.25rem) var(--page-gutter) var(--section-space);
   background:
-    radial-gradient(circle at top, rgba(255, 255, 255, 0.18), transparent 34%),
-    linear-gradient(180deg, transparent, rgba(60, 47, 28, 0.03) 100%);
+    radial-gradient(circle at top, color-mix(in srgb, var(--surface-2) 18%, transparent), transparent 52%),
+    linear-gradient(180deg, transparent, color-mix(in srgb, var(--page-bg) 88%, var(--surface-muted) 12%) 100%),
+    var(--page-bg);
 }
 
 .thread-shell {
@@ -250,12 +251,12 @@
   background:
     repeating-linear-gradient(
       180deg,
-      rgba(120, 96, 63, 0.03) 0,
-      rgba(120, 96, 63, 0.03) 1px,
+      color-mix(in srgb, var(--accent) 8%, transparent) 0,
+      color-mix(in srgb, var(--accent) 8%, transparent) 1px,
       transparent 1px,
       transparent 2.7rem
     ),
-    linear-gradient(180deg, rgba(255, 255, 255, 0.1), transparent 20%),
+    linear-gradient(180deg, color-mix(in srgb, var(--surface-muted) 65%, transparent), transparent 20%),
     var(--surface-1);
   box-shadow: 0 20px 40px rgba(40, 30, 17, 0.04);
 }
@@ -287,7 +288,7 @@
   width: 100%;
   border-radius: 1.15rem;
   border: 1px solid var(--border-soft);
-  background: rgba(255, 252, 246, 0.52);
+  background: var(--surface-1);
   color: var(--text-primary);
   padding: 1rem 1.1rem;
   outline: none;
@@ -297,8 +298,8 @@
 .thread-input:focus,
 .thread-textarea:focus {
   border-color: var(--accent-border);
-  background: rgba(255, 252, 246, 0.78);
-  box-shadow: 0 0 0 3px rgba(70, 80, 107, 0.08);
+  background: var(--surface-2);
+  box-shadow: 0 0 0 3px color-mix(in srgb, var(--accent) 14%, transparent);
 }
 
 .thread-input::placeholder,
@@ -343,24 +344,37 @@
 }
 
 .thread-list-surface {
-  border-top: 1px solid var(--border-soft);
+  display: grid;
+  gap: 0.85rem;
+  padding-top: 0.75rem;
 }
 
 .thread-list-item {
-  border-bottom: 1px solid var(--border-soft);
+  border: 1px solid var(--border-soft);
+  border-radius: var(--radius-lg);
+  background:
+    linear-gradient(180deg, rgba(255, 255, 255, 0.06), transparent 70%),
+    var(--surface-1);
+  box-shadow: 0 18px 44px rgba(28, 20, 12, 0.06);
+  overflow: hidden;
+  transition: transform var(--transition-fast), border-color var(--transition-fast), box-shadow var(--transition-fast);
+}
+
+.thread-list-item:hover {
+  transform: translateY(-2px);
+  border-color: var(--border-strong);
+  box-shadow: 0 26px 60px rgba(28, 20, 12, 0.08);
 }
 
 .thread-list-button {
   width: 100%;
-  padding: 1.5rem 0;
+  padding: 1.05rem 1.1rem;
   text-align: left;
-  transition: background-color var(--transition-fast), padding-left var(--transition-fast), padding-right var(--transition-fast);
+  transition: background-color var(--transition-fast);
 }
 
 .thread-list-button:hover {
-  background: rgba(255, 255, 255, 0.04);
-  padding-left: 0.25rem;
-  padding-right: 0.25rem;
+  background: var(--surface-muted);
 }
 
 .thread-list-title {
@@ -398,8 +412,8 @@
   gap: 0.35rem;
   padding: 0.15rem 0.55rem;
   border-radius: 999px;
-  border: 1px solid rgba(128, 128, 128, 0.22);
-  background: rgba(255, 255, 255, 0.04);
+  border: 1px solid var(--border-soft);
+  background: var(--surface-muted);
 }
 
 .resonance-pill svg {
@@ -410,13 +424,13 @@
   min-height: 2rem;
   padding: 0 0.6rem;
   border-radius: 999px;
-  border: 1px solid rgba(128, 128, 128, 0.2);
+  border: 1px solid var(--border-soft);
   background: transparent;
   line-height: 1;
 }
 
 .like-button:hover {
-  background: rgba(255, 255, 255, 0.04);
+  background: var(--surface-muted);
 }
 
 .like-button.is-liked {
diff --git a/frontend/src/pages/LandingPage.css b/frontend/src/pages/LandingPage.css
index 78933bd..28916b0 100644
--- a/frontend/src/pages/LandingPage.css
+++ b/frontend/src/pages/LandingPage.css
@@ -192,6 +192,14 @@
   align-items: center;
 }
 
+.home-hero-actions .btn-primary {
+  box-shadow: 0 14px 34px rgba(28, 20, 12, 0.12);
+}
+
+.home-hero-actions .btn-primary:hover {
+  transform: translateY(-1px);
+}
+
 @keyframes home-hero-enter {
   from {
     opacity: 0;
@@ -907,3 +915,278 @@
     align-items: flex-start;
   }
 }
+
+/* --- Sahas Home UI refresh (ported, logic preserved) --- */
+.home-shell {
+  gap: clamp(4.5rem, 8vw, 5rem);
+}
+
+.home-hero {
+  position: relative;
+  padding: clamp(2.8rem, 7vw, 3.9rem) 0;
+  isolation: isolate;
+  display: block;
+}
+
+.home-hero::before {
+  content: '';
+  position: absolute;
+  inset: 8% 2% 12%;
+  z-index: -1;
+  border-radius: 2rem;
+  background: radial-gradient(circle at center, color-mix(in srgb, var(--surface-2) 10%, transparent), transparent 74%);
+  opacity: 0.2;
+  pointer-events: none;
+}
+
+.home-hero-copy.home-hero-centered {
+  width: min(100%, 720px);
+  margin: 0 auto;
+  display: grid;
+  gap: 0.7rem;
+  justify-items: center;
+  text-align: center;
+  padding: clamp(2.8rem, 6vw, 5rem) clamp(1.1rem, 3vw, 1.8rem);
+  border-radius: 1rem;
+  background: linear-gradient(
+    to bottom,
+    color-mix(in srgb, var(--surface-1) 78%, var(--page-bg) 22%),
+    color-mix(in srgb, var(--surface-2) 88%, var(--surface-1) 12%)
+  );
+  border: 1px solid var(--border-soft);
+  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
+}
+
+.home-title {
+  color: var(--text-primary);
+  font-size: clamp(2.35rem, 5vw, 3.85rem);
+  line-height: 1.02;
+  letter-spacing: -0.02em;
+  max-width: 16ch;
+}
+
+.home-subtitle {
+  color: var(--text-secondary);
+  font-size: clamp(1.02rem, 0.98rem + 0.35vw, 1.22rem);
+  line-height: 1.75;
+}
+
+.home-trust-line {
+  margin: 0.3rem 0 0;
+  color: var(--text-muted);
+  font-size: var(--font-size-1);
+  line-height: 1.6;
+}
+
+.home-hero-divider {
+  height: 1px;
+  background: var(--border-soft);
+  width: min(100%, var(--page-max-width));
+  margin: 0 auto;
+  opacity: 0.75;
+}
+
+.surface-card {
+  border-radius: var(--radius-lg);
+  border: 1px solid var(--border-soft);
+  background: var(--surface-1);
+  box-shadow: var(--shadow-panel);
+}
+
+.home-progress {
+  margin-top: clamp(0.6rem, 2vw, 1rem);
+}
+
+.home-progress-priority {
+  margin-top: clamp(1.2rem, 3.2vw, 2.1rem);
+}
+
+.home-shelf-section {
+  padding-top: 3.25rem;
+}
+
+.home-resume.surface-card,
+.home-callout.surface-card {
+  padding: 1.1rem 1.15rem;
+}
+
+.home-resume.surface-card {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 1rem;
+}
+
+.home-resume-cover {
+  width: 4.25rem;
+  height: 4.25rem;
+  border-radius: 0.75rem;
+  overflow: hidden;
+  flex: 0 0 auto;
+  box-shadow: 0 8px 18px rgba(28, 20, 12, 0.16);
+}
+
+.home-resume-copy {
+  display: grid;
+  gap: 0.16rem;
+  flex: 1;
+  min-width: 0;
+}
+
+.home-resume-progress {
+  font-size: 0.8rem;
+  color: var(--text-muted);
+}
+
+.home-callout.surface-card {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 1rem;
+}
+
+.home-live-kicker {
+  color: var(--text-muted);
+  font-size: 0.7rem;
+  letter-spacing: 0.14em;
+  text-transform: uppercase;
+}
+
+.home-discussions {
+  display: grid;
+  grid-template-columns: repeat(3, minmax(0, 1fr));
+  gap: 1rem;
+}
+
+.home-discussion-card {
+  border-radius: var(--radius-lg);
+  border: 1px solid var(--border-soft);
+  background: var(--surface-1);
+  padding: 1.05rem 1.1rem;
+  display: grid;
+  gap: 0.35rem;
+  box-shadow: 0 10px 26px rgba(0, 0, 0, 0.08);
+}
+
+.home-discussion-card-live {
+  display: inline-flex;
+  align-items: center;
+  gap: 0.4rem;
+  font-size: 0.72rem;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  color: var(--text-muted);
+}
+
+.home-live-dot {
+  width: 0.45rem;
+  height: 0.45rem;
+  border-radius: 999px;
+  background: var(--accent);
+  opacity: 0.75;
+}
+
+.home-discussion-card-cta {
+  margin-top: 0.15rem;
+  color: var(--accent-ink);
+  font-weight: 500;
+}
+
+.home-how-it-works {
+  margin-top: clamp(2.6rem, 6vw, 4rem);
+}
+
+.home-how-heading {
+  color: var(--text-primary);
+  font-size: 0.8rem;
+  letter-spacing: 0.14em;
+  text-transform: uppercase;
+  margin-bottom: 1rem;
+}
+
+.home-how-grid {
+  display: grid;
+  grid-template-columns: repeat(3, minmax(0, 1fr));
+  gap: 1rem;
+}
+
+.home-how-card {
+  position: relative;
+  border-radius: var(--radius-lg);
+  border: 1px solid var(--border-soft);
+  background: var(--surface-1);
+  padding: 1.25rem 1.25rem 1.1rem;
+  box-shadow: var(--shadow-panel);
+  display: grid;
+  gap: 0.35rem;
+  opacity: 0;
+  transform: translateY(20px);
+  transition: opacity 520ms ease-out, transform 520ms ease-out;
+  will-change: opacity, transform;
+}
+
+.home-how-grid.is-in-view .home-how-card {
+  opacity: 1;
+  transform: translateY(0);
+}
+
+.home-how-step {
+  position: absolute;
+  top: 0.9rem;
+  right: 1rem;
+  font-size: 0.75rem;
+  color: var(--text-muted);
+}
+
+.home-how-icon {
+  width: 2.2rem;
+  height: 2.2rem;
+  border-radius: 999px;
+  display: grid;
+  place-items: center;
+  background: var(--surface-muted);
+  color: var(--accent-ink);
+}
+
+.home-how-card h3 {
+  margin-top: 0.2rem;
+  color: var(--text-primary);
+}
+
+.home-how-card p {
+  margin: 0;
+  color: var(--text-secondary);
+  line-height: 1.6;
+}
+
+.home-featured-overlay {
+  position: absolute;
+  left: 50%;
+  bottom: 0.75rem;
+  transform: translateX(-50%) translateY(6px);
+  padding: 0.35rem 0.7rem;
+  border-radius: 999px;
+  font-size: 0.72rem;
+  letter-spacing: 0.02em;
+  color: #fff;
+  background: rgba(15, 12, 10, 0.62);
+  border: 1px solid rgba(255, 255, 255, 0.24);
+  opacity: 0;
+  transition: opacity 220ms ease, transform 220ms ease;
+  pointer-events: none;
+}
+
+.home-featured-cover:hover .home-featured-overlay {
+  opacity: 1;
+  transform: translateX(-50%) translateY(0);
+}
+
+@media (max-width: 980px) {
+  .home-discussions {
+    grid-template-columns: 1fr;
+  }
+
+  .home-how-grid {
+    grid-template-columns: 1fr;
+  }
+}
diff --git a/frontend/src/pages/LandingPage.jsx b/frontend/src/pages/LandingPage.jsx
index c89f118..180a207 100644
--- a/frontend/src/pages/LandingPage.jsx
+++ b/frontend/src/pages/LandingPage.jsx
@@ -1,6 +1,6 @@
-﻿import React, { useEffect, useMemo, useState } from 'react';
+import React, { useEffect, useMemo, useRef, useState } from 'react';
 import { Link } from 'react-router-dom';
-import { BookOpen, MessageSquare, MoveRight, Users } from 'lucide-react';
+import { BookOpen, Check, MessageSquare, MoveRight, Users } from 'lucide-react';
 import api from '../utils/api';
 import { getFallbackBooks } from '../utils/bookFallback';
 import { getLibraryState } from '../utils/readingSession';
@@ -22,10 +22,11 @@ const renderCover = (book) => (
 );
 
 const FeaturedBook = ({ book, isMember }) => (
-  <Link to={isMember ? `/read/${getBookId(book)}` : "/auth"} className="home-featured-link" aria-label={`Open ${book.title}`}>
+  <Link to={isMember ? `/read/${getBookId(book)}` : '/auth'} className="home-featured-link" aria-label={`Open ${book.title}`}>
     <article className="home-featured-book">
       <div className="home-featured-cover" style={{ '--book-accent': book.coverColor || '#6f614d' }}>
         {renderCover(book)}
+        <span className="home-featured-overlay" aria-hidden="true">Start reading →</span>
       </div>
       <div className="home-featured-copy">
         <h3 className="font-serif">{book.title}</h3>
@@ -36,24 +37,48 @@ const FeaturedBook = ({ book, isMember }) => (
 );
 
 const DiscussionEntry = ({ thread }) => (
-  <Link to={`/thread/${thread.bookId}#${thread._id}`} className="home-discussion-link">
-    <article className="home-discussion">
-      <div className="home-discussion-context">
-        <span>{thread.bookTitle}</span>
-        <span className="home-discussion-divider" aria-hidden="true">/</span>
-        <span>{thread.replyCount} replies</span>
+  <Link to={`/thread/${thread.bookId}#${thread._id}`} className="home-discussion-card-link">
+    <article className="home-discussion-card">
+      <div className="home-discussion-card-live">
+        <span className="home-live-dot" aria-hidden="true" />
+        <span>Active</span>
       </div>
       <h3 className="font-serif">{thread.title}</h3>
-      <p>{thread.chapterReference?.trim() || 'Whole book'}</p>
+      <p>{thread.bookTitle || 'Shared reading'} · {thread.replyCount} replies</p>
+      <span className="home-discussion-card-cta">Open →</span>
     </article>
   </Link>
 );
 
+const howItWorksSteps = [
+  { key: 'read', title: 'Read in silence', description: 'A quiet reading space designed for focus.', icon: BookOpen },
+  { key: 'finish', title: 'Mark the moment', description: 'Finish the book to unlock its discussion room.', icon: Check },
+  { key: 'discuss', title: 'Enter the room', description: 'Join conversations with readers who finished it too.', icon: Users },
+];
+
+const getResumeProgressLabel = (book) => {
+  if (!book) {
+    return 'Continue from where you left off.';
+  }
+
+  if (typeof book.progressPercent === 'number') {
+    return `${Math.round(book.progressPercent)}% complete`;
+  }
+
+  if (typeof book.progress === 'number') {
+    return `${Math.round(book.progress)}% complete`;
+  }
+
+  return 'Continue from where you left off.';
+};
+
 export default function LandingPage({ currentUser }) {
   const [books, setBooks] = useState([]);
   const [loading, setLoading] = useState(true);
   const [sampleThreads, setSampleThreads] = useState([]);
   const [threadError, setThreadError] = useState(false);
+  const [isHowInView, setIsHowInView] = useState(false);
+  const howItWorksRef = useRef(null);
 
   const isMember = Boolean(currentUser && !currentUser.isAnonymous);
   const threadPreviewCount = isMember ? 2 : 6;
@@ -85,28 +110,32 @@ export default function LandingPage({ currentUser }) {
     return libraryState.continueReading[0] || libraryState.recentlyOpened[0] || null;
   }, [books, isMember]);
 
-  const previewThread = useMemo(() => sampleThreads[0] || null, [sampleThreads]);
-
-  const previewBook = useMemo(() => {
-    if (resumeBook) {
-      return resumeBook;
-    }
-
-    if (previewThread) {
-      const match = books.find((book) => String(getBookId(book)) === String(previewThread.bookId));
-      if (match) {
-        return match;
-      }
-    }
-
-    return featuredBooks[0] || null;
-  }, [books, featuredBooks, previewThread, resumeBook]);
-
   useEffect(() => {
     setSampleThreads([]);
     setThreadError(false);
   }, [books, threadPreviewCount]);
 
+  useEffect(() => {
+    const section = howItWorksRef.current;
+    if (!section) {
+      return undefined;
+    }
+
+    const observer = new IntersectionObserver(
+      (entries) => {
+        entries.forEach((entry) => {
+          if (entry.isIntersecting) {
+            setIsHowInView(true);
+          }
+        });
+      },
+      { threshold: 0.2 },
+    );
+
+    observer.observe(section);
+    return () => observer.disconnect();
+  }, []);
+
   if (loading) {
     return (
       <div className="home-page animate-fade-in">
@@ -121,10 +150,10 @@ export default function LandingPage({ currentUser }) {
     <div className="home-page animate-fade-in">
       <div className="home-shell">
         <header className="home-hero" aria-label="Home">
-          <div className="home-hero-copy">
+          <div className="home-hero-copy home-hero-centered">
             <h1 className="home-title font-serif">
-              <span className="home-title-line">Close the cover.</span>
-              <span className="home-title-line">Open the conversation.</span>
+              <span className="home-title-line">Finish the book.</span>
+              <span className="home-title-line">Enter the conversation.</span>
             </h1>
 
             <p className="home-subtitle">
@@ -141,89 +170,60 @@ export default function LandingPage({ currentUser }) {
               <Link to="/threads" className="btn-secondary">Explore discussions</Link>
             </div>
 
-            <p className="home-signin-hint">
-              Rooms unlock per book after a quick 5â€‘question check{!isMember ? <> â€” <Link to="/auth">sign in</Link> to keep your place</> : null}.
-            </p>
+            <p className="home-trust-line">No noise. No spoilers. Just meaningful conversations.</p>
+
+            {!isMember && (
+              <p className="home-signin-hint">
+                Rooms unlock per book after a quick 5-question check — <Link to="/auth">sign in</Link> to keep your place across visits.
+              </p>
+            )}
           </div>
+        </header>
 
-          <aside className="home-hero-visual" aria-hidden="true">
-            <div className="home-visual-surface">
-              <div className="home-visual-page">
-                <div className="home-visual-kicker">Reading Room</div>
-                <div className="home-visual-lines">
-                  <span className="home-visual-line is-title">{previewBook?.title || 'Your reading desk'}</span>
-                  <span className="home-visual-line is-body">{previewBook?.author ? `by ${previewBook.author}` : 'Pick up where you left off'}</span>
-                  <span className="home-visual-line is-body">Saved note: {'\u201C'}small details, big shift\u2026{'\u201D'}</span>
-                  <span className="home-visual-line is-body">Next: turn one page</span>
-                </div>
-              </div>
+        <div className="home-hero-divider" aria-hidden="true" />
 
-              <div className="home-visual-thread">
-                <div className="home-visual-kicker">Discussion</div>
-                <div className="home-visual-lines">
-                  <span className="home-visual-line is-title">{previewThread?.title || 'A thread from the room'}</span>
-                  <span className="home-visual-line is-body">{previewBook?.title ? previewBook.title : (previewThread?.bookTitle || 'Shared reading')}</span>
-                  <span className="home-visual-line is-body">{threadError ? 'Latest notes unavailable' : (previewThread ? `${previewThread.replyCount} replies so far` : 'Pulling in fresh notes\u2026')}</span>
-                </div>
-                <div className="home-visual-bubble">
-                  <span className="home-visual-bubble-text">{'\u201C'}That ending felt like a dare.{'\u201D'}</span>
-                </div>
-                <div className="home-visual-bubble is-me">
-                  <span className="home-visual-bubble-text">{'\u201C'}I went back and caught new clues.{'\u201D'}</span>
-                </div>
-                <div className="home-visual-bubble is-small">
-                  <span className="home-visual-bubble-text">{'\u201C'}Did anyone else notice the refrain?{'\u201D'}</span>
-                </div>
+        <section className="home-progress home-progress-priority" aria-label="Continue reading">
+          {isMember && resumeBook ? (
+            <div className="home-resume surface-card">
+              <div className="home-resume-cover" style={{ '--book-accent': resumeBook.coverColor || '#6f614d' }}>
+                {renderCover(resumeBook)}
+              </div>
+              <div className="home-resume-copy">
+                <span className="home-resume-kicker">Continue reading</span>
+                <h2 className="font-serif">{resumeBook.title}</h2>
+                <p>{resumeBook.author || 'Unknown author'}</p>
+                <span className="home-resume-progress">{getResumeProgressLabel(resumeBook)}</span>
               </div>
+              <Link to={`/read/${getBookId(resumeBook)}`} className="btn-primary sm">
+                Resume <MoveRight size={16} />
+              </Link>
             </div>
-          </aside>
-        </header>
-
-        {isMember && resumeBook && (
-          <section className="home-resume" aria-label="Resume reading">
-            <div className="home-resume-copy">
-              <span className="home-resume-kicker">Resume</span>
-              <h2 className="font-serif">Continue with {resumeBook.title}</h2>
-              <p>Return to the page you last left open.</p>
+          ) : isMember ? (
+            <div className="home-callout surface-card">
+              <div className="home-callout-copy">
+                <h2 className="font-serif">Pick up a book.</h2>
+                <p>Choose a story from your desk. Discussion rooms unlock after you finish.</p>
+              </div>
+              <Link to="/desk" className="btn-primary sm">Go to desk</Link>
             </div>
-            <Link to={`/read/${getBookId(resumeBook)}`} className="btn-secondary sm">
-              Continue <MoveRight size={16} />
-            </Link>
-          </section>
-        )}
-
-        <section className="home-intro" aria-label="What happens here">
-          <div className="home-intro-head">
-            <h2 className="font-serif">Reading should not end when the book ends.</h2>
-            <p>The real conversation begins after the last page.</p>
-          </div>
-
-          <div className="home-intro-grid" role="list">
-            <article className="home-intro-card" role="listitem">
-              <div className="home-intro-icon"><BookOpen size={18} /></div>
-              <h3 className="font-serif">Read</h3>
-              <p>A quiet, single-column interface that keeps the book in front of you.</p>
-            </article>
-            <article className="home-intro-card" role="listitem">
-              <div className="home-intro-icon"><Users size={18} /></div>
-              <h3 className="font-serif">Meet</h3>
-              <p>Find readers who just finished the same book, with text, voice, or video.</p>
-            </article>
-            <article className="home-intro-card" role="listitem">
-              <div className="home-intro-icon"><MessageSquare size={18} /></div>
-              <h3 className="font-serif">Discuss</h3>
-              <p>Leave threads that feel like margin notes: thoughtful, slow, and spoiler-safe.</p>
-            </article>
-          </div>
+          ) : (
+            <div className="home-callout surface-card">
+              <div className="home-callout-copy">
+                <h2 className="font-serif">Keep your place.</h2>
+                <p>Sign in to save progress and unlock reader-only conversation rooms after you finish.</p>
+              </div>
+              <Link to="/auth" className="btn-primary sm">Sign in</Link>
+            </div>
+          )}
         </section>
 
-        <section className="home-section" aria-labelledby="featured-heading">
+        <section className="home-section home-shelf-section" aria-labelledby="featured-heading">
           <div className="home-section-head">
             <div className="home-section-copy">
-              <h2 id="featured-heading" className="font-serif">A small shelf to begin</h2>
-              <p>Open a book, then return when you reach the end.</p>
+              <h2 id="featured-heading" className="font-serif">A place to begin</h2>
+              <p>Pick a book. The conversation will be waiting when you return.</p>
             </div>
-            <Link to={isMember ? "/desk" : "/auth"} className="home-section-link">Open The Desk</Link>
+            <Link to={isMember ? '/desk' : '/auth'} className="home-section-link">View all</Link>
           </div>
 
           <div className="home-featured" role="list" aria-label="Featured books">
@@ -236,10 +236,11 @@ export default function LandingPage({ currentUser }) {
         <section className="home-section home-discussion-preview" aria-labelledby="sample-discussions-heading">
           <div className="home-section-head">
             <div className="home-section-copy">
+              <p className="home-live-kicker">Conversations happening right now</p>
               <h2 id="sample-discussions-heading" className="font-serif">From the discussion rooms</h2>
-              <p>Ideas readers are carrying forward right now.</p>
+              <p>Recent threads from readers who just closed the book.</p>
             </div>
-            <Link to="/threads" className="home-section-link">Browse threads</Link>
+            <Link to="/threads" className="home-section-link">Join the conversation</Link>
           </div>
 
           {sampleThreads.length > 0 ? (
@@ -260,17 +261,30 @@ export default function LandingPage({ currentUser }) {
           )}
         </section>
 
-        {!isMember && (
-          <section className="home-callout" aria-label="Sign in">
-            <div className="home-callout-copy">
-              <h2 className="font-serif">Keep your place.</h2>
-              <p>Sign in to save progress and unlock reader-only conversation rooms after you finish.</p>
-            </div>
-            <Link to="/auth" className="btn-primary sm">Sign in</Link>
-          </section>
-        )}
+        <section className="home-how-it-works" aria-labelledby="how-it-works-heading" ref={howItWorksRef}>
+          <h2 id="how-it-works-heading" className="home-how-heading">How it works</h2>
+          <div className={`home-how-grid ${isHowInView ? 'is-in-view' : ''}`.trim()} role="list" aria-label="How it works steps">
+            {howItWorksSteps.map((step, index) => {
+              const StepIcon = step.icon;
+              return (
+                <article
+                  key={step.key}
+                  className="home-how-card"
+                  role="listitem"
+                  style={{ transitionDelay: `${120 * index}ms` }}
+                >
+                  <span className="home-how-step" aria-hidden="true">{index + 1}</span>
+                  <span className="home-how-icon" aria-hidden="true">
+                    <StepIcon size={16} strokeWidth={2.1} />
+                  </span>
+                  <h3>{step.title}</h3>
+                  <p>{step.description}</p>
+                </article>
+              );
+            })}
+          </div>
+        </section>
       </div>
     </div>
   );
 }
-
diff --git a/frontend/src/pages/ReadingRoom.css b/frontend/src/pages/ReadingRoom.css
index 0a18d4a..9e181f9 100644
--- a/frontend/src/pages/ReadingRoom.css
+++ b/frontend/src/pages/ReadingRoom.css
@@ -125,6 +125,11 @@
   border-color: rgba(229, 229, 229, 0.12);
 }
 
+.theme-midnight .settings-panel {
+  background: rgba(16, 26, 51, 0.96);
+  border-color: rgba(231, 236, 255, 0.12);
+}
+
 .theme-light .toolbar-stepper,
 .theme-light .chapter-nav-btn,
 .theme-light .settings-btn,
@@ -155,6 +160,16 @@
   background: rgba(229, 229, 229, 0.06);
 }
 
+.theme-midnight .toolbar-stepper,
+.theme-midnight .chapter-nav-btn,
+.theme-midnight .settings-btn,
+.theme-midnight .font-size-toggles,
+.theme-midnight .theme-btn,
+.theme-midnight .line-height-options button,
+.theme-midnight .settings-close {
+  background: rgba(231, 236, 255, 0.06);
+}
+
 .theme-light ::selection {
   background: rgba(63, 73, 100, 0.16);
 }
@@ -167,6 +182,10 @@
   background: rgba(215, 222, 244, 0.16);
 }
 
+.theme-midnight ::selection {
+  background: rgba(121, 166, 255, 0.18);
+}
+
 .reader-toolbar,
 .reader-progress {
   position: fixed;
@@ -440,6 +459,10 @@
   color: #e5e5e5;
 }
 
+.preview-midnight {
+  color: #e7ecff;
+}
+
 .font-size-toggles {
   min-height: 2.8rem;
   padding: 0.2rem;
diff --git a/frontend/src/pages/ReadingRoom.jsx b/frontend/src/pages/ReadingRoom.jsx
index d6bf5b9..787357c 100644
--- a/frontend/src/pages/ReadingRoom.jsx
+++ b/frontend/src/pages/ReadingRoom.jsx
@@ -1,4 +1,4 @@
-﻿import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
+import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
 import { useParams, useNavigate, Link } from 'react-router-dom';
 import {
   ArrowRight,
@@ -11,6 +11,7 @@ import {
 import api from '../utils/api';
 import { getFallbackBookById } from '../utils/bookFallback';
 import { trackBookOpened, updateReadingSession } from '../utils/readingSession';
+import { UI_THEMES } from '../utils/uiThemes';
 import { PaginationEngine } from '../components/reader/PaginationEngine';
 import PageRenderer from '../components/reader/PageRenderer';
 import './ReadingRoom.css';
@@ -794,30 +795,19 @@ const ReadingRoom = ({ uiTheme, onThemeChange }) => {
               <>
                 <div className="settings-group">
                   <span className="settings-label">Theme</span>
-              <div className="theme-toggles">
-                <button
-                  type="button"
-                  className={`theme-btn preview-light ${uiTheme === 'light' ? 'active' : ''}`}
-                  onClick={() => onThemeChange('light')}
-                >
-                  Light
-                </button>
-                <button
-                  type="button"
-                  className={`theme-btn preview-sepia ${uiTheme === 'sepia' ? 'active' : ''}`}
-                  onClick={() => onThemeChange('sepia')}
-                >
-                  Sepia
-                </button>
-                <button
-                  type="button"
-                  className={`theme-btn preview-dark ${uiTheme === 'dark' ? 'active' : ''}`}
-                  onClick={() => onThemeChange('dark')}
-                >
-                  Dark
-                </button>
-              </div>
-            </div>
+                  <div className="theme-toggles">
+                    {UI_THEMES.map((theme) => (
+                      <button
+                        key={theme.id}
+                        type="button"
+                        className={`theme-btn preview-${theme.id} ${uiTheme === theme.id ? 'active' : ''}`}
+                        onClick={() => onThemeChange(theme.id)}
+                      >
+                        {theme.label}
+                      </button>
+                    ))}
+                  </div>
+                </div>
 
             <div className="settings-group">
               <span className="settings-label">Text size</span>
diff --git a/frontend/src/pages/SettingsPage.jsx b/frontend/src/pages/SettingsPage.jsx
index 6d3d05c..387f68a 100644
--- a/frontend/src/pages/SettingsPage.jsx
+++ b/frontend/src/pages/SettingsPage.jsx
@@ -1,13 +1,8 @@
 import React from 'react';
 import { Check } from 'lucide-react';
+import { UI_THEMES } from '../utils/uiThemes';
 import './SettingsPage.css';
 
-const themeOptions = [
-  { id: 'light', label: 'Light' },
-  { id: 'sepia', label: 'Sepia' },
-  { id: 'dark', label: 'Dark' },
-];
-
 const SettingsPage = ({ uiTheme, onThemeChange }) => (
   <div className="settings-page animate-fade-in">
     <header className="settings-head">
@@ -18,7 +13,7 @@ const SettingsPage = ({ uiTheme, onThemeChange }) => (
     <section className="settings-card glass-panel" aria-label="Appearance">
       <div className="settings-kicker">Appearance</div>
       <div className="settings-row" role="group" aria-label="Theme">
-        {themeOptions.map((option) => (
+        {UI_THEMES.map((option) => (
           <button
             key={option.id}
             type="button"
diff --git a/frontend/src/pages/ThreadAccessHub.css b/frontend/src/pages/ThreadAccessHub.css
index a0dbe47..53d5d8e 100644
--- a/frontend/src/pages/ThreadAccessHub.css
+++ b/frontend/src/pages/ThreadAccessHub.css
@@ -97,6 +97,20 @@
   min-height: 6.1rem;
   border-radius: var(--radius-lg);
   box-shadow: none;
+  transition:
+    transform var(--transition-fast),
+    border-color var(--transition-fast),
+    background-color var(--transition-fast),
+    box-shadow var(--transition-fast);
+  background:
+    linear-gradient(180deg, rgba(255, 255, 255, 0.04), transparent 60%),
+    var(--surface-1);
+}
+
+.thread-access-card:hover {
+  transform: translateY(-2px);
+  border-color: var(--border-strong);
+  box-shadow: 0 16px 34px rgba(28, 20, 12, 0.09);
 }
 
 .thread-access-mini-cover {
@@ -149,32 +163,35 @@
   padding: 0.2rem 0.52rem;
   border-radius: var(--radius-pill);
   font-size: 0.68rem;
-  border: 1px solid rgba(255, 249, 240, 0.24);
+  border: 1px solid var(--border-soft);
+  background: var(--surface-muted);
+  color: var(--text-secondary);
 }
 
 .thread-status.unlocked {
-  background: rgba(223, 232, 221, 0.24);
+  background: var(--success-soft);
   color: var(--text-primary);
 }
 
 .thread-status.quiz-required {
-  background: rgba(223, 227, 237, 0.22);
+  background: var(--accent-soft);
   color: var(--text-primary);
 }
 
 .thread-status.read-required {
-  background: rgba(236, 223, 190, 0.24);
+  background: var(--warning-soft);
   color: var(--text-primary);
 }
 
 .thread-access-title {
   margin: 0;
-  font-size: 0.98rem;
+  font-size: 1rem;
   line-height: 1.2;
   display: -webkit-box;
   -webkit-line-clamp: 2;
   -webkit-box-orient: vertical;
   overflow: hidden;
+  color: var(--text-primary);
 }
 
 .thread-access-author {
